#!/usr/bin/env bash

set -eu

source "$(dirname "$(realpath "$0")")"/../global-vars.inc.bash

PHP_PATH_NAME="${ROOT_PATH}"/bin/dev/start.php \
    source "${ROOT_PATH}"/bin/parallel-processes.inc.bash
